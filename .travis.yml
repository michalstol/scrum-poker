branches:
    only:
        - master
langauge: node_js
node_js:
    - '12.16.1'
install:
    - npm install
script:
    - CI=false npm run build
after_success:
    - firebase deploy --token "$FIREBASE_TOKEN"
